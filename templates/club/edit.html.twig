{% extends 'base.html.twig' %}

{% block title %}Edit Club{% endblock %}

{% block body %}
<div class="container d-block mt-5">
    <div class="row">
        <h1>Club</h1>
    </div>
    <div class="row">
        {{ include('club/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

        <a href="{{ path('app_club_index') }}" class="btn btn-sm btn-info m-1">back to list</a>

        {{ include('club/_delete_form.html.twig') }}
    </div>    
</div>
{% endblock %}
